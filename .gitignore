node_modules/
dist/
build/
.DS_Store
*.log
*.tsbuildinfo
.env
.env.local

# Local data (datasets + comparison results live on disk)
data/

# X data archive (DMs, IP log, phone number) — only ever the import's input
tweets/
