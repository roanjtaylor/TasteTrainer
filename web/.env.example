# Copy to web/.env.local for local dev; set the same names in the Vercel project.
# Both are PUBLIC values (Supabase dashboard -> Project Settings -> API Keys).
VITE_SUPABASE_KEY=sb_publishable_...
# Optional — defaults to the Curiosity project (web/src/lib/supabase.ts).
# VITE_SUPABASE_URL=https://<project-ref>.supabase.co
