{
  "name": "tastetrainer",
  "version": "0.1.0",
  "private": true,
  "description": "A personal, local-first tool for training taste by exposure to the best work in a field — AI-assisted curation + 1v1 Elo ranking. Built from /plan.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
