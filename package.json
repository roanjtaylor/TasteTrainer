{
  "name": "tastetrainer",
  "version": "0.1.0",
  "private": true,
  "description": "A personal, local-first tool for training taste by exposure to the best work in a field — AI-assisted curation and browsing. Built from /plan.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server --silent\" \"npm run dev -w web --silent\"",
    "build": "npm run build -w web",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
