---
description: Review a whole world — its map, its missing fields, and which datasets should merge, split or be renamed
---
Review this world as a whole.

Read the curation rules (the field-map sections), the shelf, and the world's map. Then answer three things:

1. **Is the map still right?** Do the axes genuinely organise this world, and does every dataset sit in the right region? Change only what is wrong.
2. **Which fields am I missing?** Think about the world's objective reality — every region of the map should hold the fields that actually make it up. Favour the fields I wouldn't think of over the obvious ones. Add them to the map as proposed missing fields, each with a one-line description and why it matters.
3. **Which datasets should be merged, split or renamed?** Two that are really one field, one that is really two, a name that misleads. Propose the restructuring as staged work (create, move, delete the emptied field).

Give me the verdict first in a few lines, then stage the changes.

$ARGUMENTS
