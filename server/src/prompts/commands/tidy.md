---
description: Review the subtopics and filing — merge, split, rename, re-file, and remove duplicates
---
Tidy this dataset's structure.

Read it in full and the curation rules (especially the subtopic rules). Then judge:

- Are the subtopics the smallest set of distinct categories that together cover the whole field? Merge two that don't hold a real distinction; split one that is really two things; rename any that mislead. Never pad to a number.
- Is every item filed where it belongs? Re-file the ones that aren't.
- Are there duplicates or near-duplicates (the same work under two names, a variant beside its original)? Propose removing the lesser one, and say why.
- Is anything here not actually of this field at all? Say so and propose a move or a removal.

Stage the structural change first (with subtopic renames so items follow), then the re-filings, then the removals. Keep what is right; a settled structure is something I have learned my way around.

$ARGUMENTS
